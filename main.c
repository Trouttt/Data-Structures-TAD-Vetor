int main(){
    createSet(); //cria um conjunto.
    insertElement(); //insere um elemento no conjunto.
    removeElement(); // remove um elemento do conjunto.
    testNumber(); //testa se um número pertence ao conjunto.
    lowerValue(); //verifica se é o menor valor.
    unionSet(); //esta função irá unir os dois conjuntos
    testSameSet(); //esta função irá verificar 
    testEmptySet(); //verifica se é um conjunto vazio
}